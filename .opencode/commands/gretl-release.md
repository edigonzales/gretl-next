Use the `gretl-runtime-image-release` skill from `.agents/skills/gretl-runtime-image-release/SKILL.md`.

Inspect the requested runtime image, versioning, publishing, or release-prep change, choose the narrowest useful verification first, and require explicit human approval before publishing snapshots or running external release actions.
