Use the `gretl-pr-review` skill from `.agents/skills/gretl-pr-review/SKILL.md`.

Review or summarize the current GRETL diff. For review requests, put findings first with file and line references. For PR prep, include behavior changed, module boundary impact, public API or DSL impact, tests run, skipped checks, and residual risk.
