pluginManagement {
    repositories {
        maven {
            url = 'https://jars.interlis.guru/snapshots'
        }
        gradlePluginPortal()
    }
}
rootProject.name = 'duckdb-sql-executor-gpkg-only'
